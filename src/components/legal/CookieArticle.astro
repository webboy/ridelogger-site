---
import type { LegalBundle } from '../../i18n/loadLegal';
import { renderCookieArticleHtml } from '../../utils/renderCookieArticleHtml';

interface Props {
	legal: LegalBundle;
	privacyUrl: string;
}

const { legal, privacyUrl } = Astro.props;
---

<div set:html={renderCookieArticleHtml(legal, privacyUrl)} />
