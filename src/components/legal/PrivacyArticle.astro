---
import type { LegalBundle } from '../../i18n/loadLegal';
import { renderPrivacyArticleHtml } from '../../utils/renderPrivacyArticleHtml';

interface Props {
	legal: LegalBundle;
}

const { legal } = Astro.props;
---

<div set:html={renderPrivacyArticleHtml(legal)} />
